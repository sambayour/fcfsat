const initState = "http://127.0.0.1:8000/api/";
const MainUrl = (state=initState,action)=>{
    switch (action.type) {
        case "CHANGE":
             break;
    
        default:
            return state;
    }
}

export default MainUrl;